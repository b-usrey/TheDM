services:
  thedm:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      MAPGEN_DATA_DIR: /data
    volumes:
      - ./data:/data
    ports:
      - "5000:8000"

  # Future services (media server, home automation, etc.) can join this
  # file as additional entries here.
